---
title: "Get User Delegations"
api: "GET /api/v1/auth/delegation"
description: "Retrieve the user's stored delegations"
---

Retrieves the user's stored delegations.

Returns a paginated list of all delegations created by the authenticated user, including both active and inactive delegations.

### Headers

<ParamField header="Authorization" type="string" required>
  Bearer token (JWT) from OAuth2 login
</ParamField>

### Query Parameters

<ParamField query="skip" type="integer" default={0}>
  Number of records to skip for pagination
</ParamField>

<ParamField query="limit" type="integer" default={10}>
  Maximum number of records to return (max: 100)
</ParamField>

### Response

<ResponseField name="delegations" type="array">
  Array of delegation objects
  
  <Expandable title="Delegation Object">
    <ResponseField name="id" type="integer">
      Unique identifier for the delegation
    </ResponseField>
    
    <ResponseField name="delegate_address" type="string">
      The delegated blockchain address
    </ResponseField>
    
    <ResponseField name="signed_delegation_data" type="string">
      The signed delegation data
    </ResponseField>
    
    <ResponseField name="expiry" type="string">
      Expiration timestamp (null if no expiry)
    </ResponseField>
    
    <ResponseField name="is_active" type="boolean">
      Whether the delegation is currently active
    </ResponseField>
    
    <ResponseField name="user_id" type="integer">
      Your user ID
    </ResponseField>
    
    <ResponseField name="created_at" type="string">
      ISO 8601 timestamp when created
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>
```bash cURL
curl --request GET \
  --url 'https://api.mor.org/api/v1/auth/delegation?skip=0&limit=10' \
  --header 'Authorization: Bearer YOUR_JWT_TOKEN'
```

```javascript JavaScript
const response = await fetch(
  'https://api.mor.org/api/v1/auth/delegation?skip=0&limit=10',
  {
    method: 'GET',
    headers: {
      'Authorization': 'Bearer YOUR_JWT_TOKEN'
    }
  }
);

const delegations = await response.json();
```

```python Python
import requests

response = requests.get(
    'https://api.mor.org/api/v1/auth/delegation',
    params={'skip': 0, 'limit': 10},
    headers={'Authorization': 'Bearer YOUR_JWT_TOKEN'}
)

delegations = response.json()
```
</RequestExample>

<ResponseExample>
```json 200 Response
[
  {
    "id": 1,
    "delegate_address": "0x1234567890abcdef1234567890abcdef12345678",
    "signed_delegation_data": "0xsigned_data_here",
    "expiry": "2024-12-31T23:59:59Z",
    "is_active": true,
    "user_id": 123,
    "created_at": "2024-01-01T00:00:00Z"
  },
  {
    "id": 2,
    "delegate_address": "0xabcdef1234567890abcdef1234567890abcdef12",
    "signed_delegation_data": "0xanother_signed_data",
    "expiry": null,
    "is_active": false,
    "user_id": 123,
    "created_at": "2024-01-05T12:00:00Z"
  }
]
```

```json 422 Validation Error
{
  "detail": [
    {
      "loc": ["query", "limit"],
      "msg": "Input should be less than or equal to 100",
      "type": "less_than_equal"
    }
  ]
}
```
</ResponseExample>

<Info>
  **Pagination**: Use the `skip` and `limit` parameters to paginate through large sets of delegations. The maximum limit per request is 100.
</Info>

<Note>
  Inactive delegations are still returned by this endpoint. Filter by `is_active` in your application if you only want active delegations.
</Note>

