---
title: "API Reference"
description: "Comprehensive API documentation for the Morpheus API Gateway"
---

## Welcome to Morpheus API Gateway

The Morpheus API Gateway connects Web2 clients to the Morpheus-Lumerin AI Marketplace. This gateway provides OpenAI-compatible endpoints for seamless integration with existing AI applications.

## Base URL

```
https://api.mor.org/api/v1/
```

## Authentication

The API supports two authentication methods:

### Bearer Token (JWT)
Used for user-facing endpoints like account management, API key generation, and settings.

```bash
Authorization: Bearer <jwt_token>
```

### API Key
Used for AI inference endpoints like chat completions and model listings.

```bash
Authorization: Bearer sk-xxxxxxxxxxxxx
```

## API Categories

- **Auth**: User authentication, API key management, private keys, and delegation
- **Models**: List and query available AI models and bids
- **Chat**: OpenAI-compatible chat completion endpoints
- **Session**: Blockchain session management for provider connections
- **Automation**: Automated session creation settings
- **Chat History**: Store and retrieve chat conversations

## Rate Limits

Rate limits are applied per API key and vary based on your account tier. Contact support for enterprise rate limits.

## Error Handling

The API returns standard HTTP status codes:

- `200` - Success
- `201` - Created
- `204` - No Content (successful deletion)
- `400` - Bad Request
- `401` - Unauthorized
- `403` - Forbidden
- `404` - Not Found
- `422` - Validation Error
- `500` - Internal Server Error

Error responses include a JSON body with details:

```json
{
  "detail": [
    {
      "loc": ["body", "field_name"],
      "msg": "Field required",
      "type": "missing"
    }
  ]
}
```

